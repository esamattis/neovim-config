#!/bin/sh

set -eu

cd ~/.vim

[ -z "$(git status --short)" ] || {
    git status
    echo
    echo
    echo "Local changes!" >&2
    exit 1
}

git pull --rebase origin master
git push sshorigin master && git fetch origin
